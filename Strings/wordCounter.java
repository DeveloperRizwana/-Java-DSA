package Strings;

import java.util.*;

public class wordCounter {
    public static void main(String[] args) {
        String[] words = str.split(" "); // Split the string into words using space as a delimiter
        return words.length; // Return the number of words
    }

    public static void main(String[] args) {
        String input = "Hello world this is ChatGPT"; // Example input
        int wordCount = countWords(input); // Call the method
        System.out.println("Total words: " + wordCount); // Output the result
    }
}
